# School lesson video

**On the lesson page:** the 9:16 reel is a **programmatic canvas animation** (`playReel` in `school/build.py`). It is not a video file.

**YouTube** Shorts + Longs live in `school/lessons.yaml` (`youtube_short` / `youtube_long`) and appear as **links under the reel** — not as an embed.

Local `*.mp4` under this folder (if any) are unused.
